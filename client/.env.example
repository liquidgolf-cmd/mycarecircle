# ── Client environment variables ─────────────────────────────────────────────
# Copy these into Vercel → Project → Settings → Environment Variables

# Railway server URL — get this after deploying the server to Railway
# It will look like: https://carecircle-server-production.up.railway.app
VITE_API_URL=https://your-railway-app.up.railway.app/api/v1

# Supabase — get from supabase.com → Project → Settings → API
# Use the same project as development
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key_here
