# ─────────────────────────────────────────────────────────────
#  The CareCircle — Environment Variables Reference
#  Copy this file to server/.env and client/.env, then fill in
#  the real values. Never commit .env files to version control.
# ─────────────────────────────────────────────────────────────

# ── SERVER (server/.env) ──────────────────────────────────────

# Node environment: "development" or "production"
NODE_ENV=development

# Port the Express server listens on
PORT=3001

# Frontend origin(s) allowed by CORS — comma-separated if multiple
# e.g. production: https://carecircle.yourdomain.com
FRONTEND_URL=http://localhost:5173

# Supabase — use the SERVICE ROLE key (server-side only, never expose to client)
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_KEY=your_supabase_service_role_key

# Anthropic Claude API — used by Willow onboarding + Catch Me Up + weekly digest
ANTHROPIC_API_KEY=sk-ant-...

# Resend — transactional email for invites and weekly digests
RESEND_API_KEY=re_...
RESEND_FROM_EMAIL=noreply@yourdomain.com


# ── CLIENT (client/.env) ──────────────────────────────────────

# Base URL of the Express API
VITE_API_URL=http://localhost:3001

# Supabase — use the ANON key (safe to expose to browser)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
